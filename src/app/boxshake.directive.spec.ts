import { BoxshakeDirective } from './boxshake.directive';

describe('BoxshakeDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxshakeDirective();
    expect(directive).toBeTruthy();
  });
});
