import { BreakDirective } from './break.directive';

describe('BreakDirective', () => {
  it('should create an instance', () => {
    const directive = new BreakDirective();
    expect(directive).toBeTruthy();
  });
});
